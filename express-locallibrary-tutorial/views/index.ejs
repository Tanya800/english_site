<!DOCTYPE html>
<html lang="en">
<%- include('partials/head.html')%>
<%- include('partials/header.html')%>
<body>
<div class="">


<div>
    <p class="text-attent">
        Start learning with funny words at the begining, and who knows,maybe you're loving English!
    </p>
</div>

<div >
    <div class="row" style="margin: 0 5% 0 5%;">

        <% tests.forEach(el=>{ %>
        <div class="col">
            <div class="card" style="width: 12rem;text-align: center;margin-bottom: 5%">
                <div>
                    <img src="/images/<%= el.picture %>" class="card-img-top" alt="picture" >
                </div>
                <div class="card-body">
                    <h5 class="card-title"><%= el.title %></h5>
                    <p class="card-text"><%= el.description %></p>
                    <a href="/test/<%= el.id %>" class="btn btn-primary">Start</a>
                </div>
            </div>
        </div>
        <% }); %>

    </div>
</div>
</div>
</body>
<%- include('partials/footer.html')%>
</html>
